#pragma once

#define SERVER_SIGERR		SIGUSR2
#define SERVER_SIGNOTIFY	SIGUSR1

typedef enum {
	SERVER_IDLE,
	SERVER_QUIT,

	SERVER_HOST,
	SERVER_UNHOST,

	SERVER_CONNECT,
	SERVER_DISCONNECT,

	SERVER_RUNNING,

	SERVER_KILLED,
	SERVER_DEAD,
} server_status_t;
